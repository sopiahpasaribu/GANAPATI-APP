# Ganapatih App - Frontend

## Setup
```
cd frontend
pnpm install
pnpm run dev
```

Frontend expects backend on http://localhost:4000 by default.
To change API base URL, create a .env with VITE_API_BASE.
