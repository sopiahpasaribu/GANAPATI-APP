# Ganapatih App - Backend (better-sqlite3)

## Setup
```
cd backend
pnpm install    # or npm install
pnpm run migrate
pnpm run dev    # or npm start
```
Default port: 4000. Copy `.env.example` to `.env` to customize.
